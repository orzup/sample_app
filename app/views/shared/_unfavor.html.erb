<% favorite = current_user.favorites.find_by(micropost_id: micropost.id) %>
<%= form_for(favorite, html: {method: :delete}, remote: true) do |f| %>
  <%= f.submit "Unfav", class: "btn btn-warning btn-small" %>
<% end %>
